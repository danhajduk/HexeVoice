# Firmware dependency locks

These ESP-IDF Component Manager lockfiles are selected by board profile,
firmware application, and required ESP-IDF version. Regenerate a lock only
with the SDK version declared by that board profile.
