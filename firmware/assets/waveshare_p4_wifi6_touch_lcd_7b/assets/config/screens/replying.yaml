id: replying
owner: backend
conditions:
  match: all
  items:
    - flag: replying
sidebars: true
buttons: {preset: standard}
items:
  - item: header_clock
  - item: activity_replay
    x: 512
    y: 295
    animations:
      - preset: activity_slide_right
        when: {flag: replying}
      - preset: soft_pulse
        when: {flag: replying}
