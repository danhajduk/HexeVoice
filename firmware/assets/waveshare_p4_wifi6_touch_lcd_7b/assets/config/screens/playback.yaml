id: playback
owner: backend
conditions:
  match: all
  items:
    - flag: playback_active
sidebars: true
buttons: {preset: standard}
items:
  - item: header_clock
  - item: activity_replay
    x: 512
    y: 295
    animations:
      - preset: activity_slide_right
        when: {flag: playback_active}
      - preset: soft_pulse
        when: {flag: playback_active}
