Task #001
Step objective: Define the canonical Phase 0 lifecycle model and surface it through the starter backend contracts.
Chosen fix: Add a dedicated onboarding model for the Core 10-step lifecycle, project the first canonical state through status/onboarding payloads, and update tests and architecture docs to match.
What changed: Added canonical onboarding step definitions, expanded API responses with current-step metadata and step lists, replaced placeholder lifecycle names with canonical Core states, and documented the lifecycle sequence.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_onboarding.py tests/test_api.py tests/test_readiness.py tests/test_scheduler.py` (6 passed); `PYTHONPATH=src .venv/bin/python` payload smoke check passed.
###########
Task #002
Step objective: Add restart-safe onboarding persistence for pre-trust setup, onboarding session metadata, trust activation metadata, and resume state.
Chosen fix: Introduce a file-backed onboarding state store under `runtime/`, resolve its path from settings, and wire the runtime/API projections to read persisted trust and current-step state.
What changed: Added onboarding persistence models and store, added settings support for the resolved onboarding-state path, wired persisted state into the runtime service and app factory, added round-trip and resume/API tests, and documented the persistence boundary.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_persistence.py tests/test_onboarding.py tests/test_api.py tests/test_config.py tests/test_readiness.py tests/test_scheduler.py` (11 passed); `PYTHONPATH=src .venv/bin/python` TestClient smoke check passed for persisted `provider_setup` resume state.
###########
Task #003
Step objective: Add backend local setup APIs for Node Identity and Core Connection and advance the local lifecycle from saved setup state.
Chosen fix: Add a dedicated onboarding state service with GET/PUT local-setup APIs backed by the onboarding store and make runtime payloads reload persisted state on demand.
What changed: Added local setup request/response models, implemented onboarding state service methods for node identity and core connection drafts, exposed new onboarding setup endpoints, advanced resume state into `core_connection` and `bootstrap_discovery`, and documented the new APIs.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_onboarding.py tests/test_api.py tests/test_persistence.py tests/test_config.py` (11 passed); `PYTHONPATH=src .venv/bin/python` TestClient smoke check confirmed saved setup projects `node_name=kitchen-voice` and `current_step_id=bootstrap_discovery`.
###########
Task #004
Step objective: Implement bootstrap discovery validation and advance the lifecycle from `bootstrap_discovery` to `registration` after a valid Core bootstrap advertisement.
Chosen fix: Add a bootstrap discovery service with connection-status persistence, contract validation for the retained Core bootstrap payload, and lifecycle advancement after a valid advertisement.
What changed: Added bootstrap discovery persistence fields, bootstrap config defaults, bootstrap discovery request/response models, new bootstrap discovery endpoints, advertisement validation against the Core contract, and tests covering persistence and lifecycle advancement.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_bootstrap_discovery.py tests/test_api.py tests/test_onboarding.py tests/test_persistence.py tests/test_config.py` (13 passed); `PYTHONPATH=src .venv/bin/python` TestClient smoke check confirmed valid bootstrap advertisement projects `current_step_id=registration`.
###########
Task #005
Step objective: Start onboarding sessions against Core from the saved local setup/bootstrap state and persist approval-session metadata.
Chosen fix: Add a small Core onboarding client and session-start service, expose a local `session/start` route, persist Core session metadata, and guard the route when local setup or bootstrap discovery is incomplete.
What changed: Added Core onboarding HTTP client, session-start service, persisted finalize/session metadata, extended onboarding status payload with approval/session fields, exposed the session-start endpoint, and added tests for guarded startup and successful session creation.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_session_start.py tests/test_api.py tests/test_bootstrap_discovery.py tests/test_onboarding.py tests/test_persistence.py tests/test_config.py` (15 passed); `PYTHONPATH=src .venv/bin/python` TestClient smoke check confirmed successful session start projects `current_step_id=approval` and `lifecycle_state=pending_approval`.
###########
Task #006
Step objective: Surface operator approval state locally, poll the Core finalize route, and persist approved activation material without promoting trust yet.
Chosen fix: Add an approval polling service backed by the Core finalize contract, extend onboarding session persistence/status metadata, expose a local `session/poll` route, and move the lifecycle to `trust_activation` only after an approved finalize outcome.
What changed: Added Core finalize client support, persisted poll timestamps/terminal outcomes/pending activation payloads, exposed `POST /api/onboarding/session/poll`, expanded onboarding status fields with approval metadata, added approval polling tests, and documented the current approval visibility boundary.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_approval_polling.py tests/test_session_start.py tests/test_api.py tests/test_bootstrap_discovery.py tests/test_onboarding.py tests/test_persistence.py tests/test_config.py` (18 passed).
###########
Task #007
Step objective: Consume approved trust activation exactly once, persist the canonical protected trust fields, and advance the node into post-trust provider setup.
Chosen fix: Add a local trust activation finalize service/route that promotes the staged approved activation payload into the trust state, clears the pending activation staging area, and moves resume state from `trust_activation` to `provider_setup`.
What changed: Expanded persisted trust activation fields to cover the canonical Core payload, added `POST /api/onboarding/trust-activation/finalize`, kept secret-bearing fields out of public API responses, added tests for one-time activation consumption and API-level trusted-state projection, and documented the trust activation boundary.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_trust_activation.py tests/test_approval_polling.py tests/test_session_start.py tests/test_api.py tests/test_bootstrap_discovery.py tests/test_onboarding.py tests/test_persistence.py tests/test_config.py` (21 passed).
###########
Task #008
Step objective: Resume trusted state safely after restart and use Core's explicit trust-status contract to detect revocation or removal without guessing from generic failures.
Chosen fix: Add a trust-status refresh service/route that calls Core with the last issued trust token, persists support metadata locally, preserves post-trust resume when still supported, and moves the node back to re-onboarding when Core reports revoked or removed trust.
What changed: Added Core trust-status client support, persisted trust support/revocation metadata, exposed `POST /api/onboarding/trust-status/refresh`, projected trust-loss details through onboarding status, added runtime blockers for revoked/removed trust, and added tests for supported resume plus revoked/removed re-onboarding behavior.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_trust_status.py tests/test_trust_activation.py tests/test_approval_polling.py tests/test_session_start.py tests/test_api.py tests/test_bootstrap_discovery.py tests/test_onboarding.py tests/test_persistence.py tests/test_config.py` (24 passed).
###########
Task #009
Step objective: Add an explicit provider setup boundary for the trusted-but-not-yet-operational state and use it to gate capability declaration.
Chosen fix: Add provider setup persistence plus `/api/providers/setup` routes, track supported and enabled providers separately from trust state, and advance the local lifecycle into `capability_declaration` only when provider selection is valid.
What changed: Added provider-setup state models, implemented provider setup service/routes under the provider namespace, projected enabled providers through the capabilities summary and provider status APIs, updated runtime blockers for `provider_setup`, and added tests for blocked and successful provider configuration paths.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_provider_setup.py tests/test_trust_status.py tests/test_trust_activation.py tests/test_approval_polling.py tests/test_session_start.py tests/test_api.py tests/test_bootstrap_discovery.py tests/test_onboarding.py tests/test_persistence.py tests/test_config.py` (27 passed).
###########
Task #010
Step objective: Implement the Core Phase 2 capability declaration, governance sync, and operational-status polling surfaces and persist their readiness metadata locally.
Chosen fix: Add Core client methods and local service boundaries for capability declaration and governance/operational polling, persist accepted capability and governance state, and let Core's `operational_ready` projection drive the final move into `ready`.
What changed: Added capability declaration persistence and route, governance current/refresh services and routes, operational-status polling and persistence, expanded public models with capability/governance/readiness fields, updated runtime status/readiness projections, and added end-to-end Phase 2 tests covering declaration through operational readiness.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_phase2.py tests/test_provider_setup.py tests/test_trust_status.py tests/test_trust_activation.py tests/test_approval_polling.py tests/test_session_start.py tests/test_api.py tests/test_bootstrap_discovery.py tests/test_onboarding.py tests/test_persistence.py tests/test_config.py tests/test_readiness.py` (31 passed).
###########
Task #011
Step objective: Expand the local status payloads so the frontend can render setup readiness and blocking detail directly from the node API instead of reconstructing it client-side.
Chosen fix: Add richer status model fields plus a structured `capability_setup` object to the local onboarding/status payloads, projecting readiness flags, provider/task setup summaries, blocking reasons, and Phase 2 readiness state from persisted node-local data.
What changed: Expanded node and onboarding status response models, added local capability-setup payload generation in the runtime service, surfaced capability/governance readiness details alongside the canonical step projection, and updated API/runtime tests to validate the richer payloads.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_onboarding.py tests/test_api.py tests/test_phase2.py tests/test_provider_setup.py tests/test_trust_status.py tests/test_trust_activation.py tests/test_approval_polling.py tests/test_session_start.py tests/test_bootstrap_discovery.py tests/test_persistence.py tests/test_config.py tests/test_readiness.py` (31 passed).
###########
Task #012
Step objective: Replace the starter frontend styling with the shared Hexe node visual foundation so later onboarding screens inherit the canonical shell and card system.
Chosen fix: Port the shared dark `--sx-*` token family and shell grammar into the frontend theme, add the missing Vite entrypoint, and lightly restructure the current app panels so they render inside the new hero, rail, and card layout.
What changed: Added canonical design tokens and base theme CSS, rebuilt the page shell and responsive layout, upgraded cards/pills/callouts/facts grids, refreshed the current placeholder panels to use the new shared classes, added `frontend/index.html`, and updated `.gitignore` for frontend build artifacts and dependencies.
Verification: `cd frontend && npm install`; `cd frontend && npm run build` (passed).
###########
Task #013
Step objective: Turn the themed frontend into a real onboarding shell that reflects the canonical setup flow and active stage instead of generic placeholder panels.
Chosen fix: Fetch onboarding status alongside node status, drive the setup sidebar from the canonical steps array, and replace the onboarding placeholder with a stage-card renderer that uses operator callouts and stage-aware status language.
What changed: Added onboarding-status fetch support, updated the app shell to use onboarding-derived step context, upgraded the status card to include onboarding metadata, replaced the onboarding placeholder with a stage-card shell for pre-trust, approval, trust activation, and post-trust states, and extended shared CSS for stage-card presentation and current/completed flow markers.
Verification: `cd frontend && npm install && npm run build` (passed).
###########
Task #014
Step objective: Implement the first three setup steps against real backend APIs so operators can save local setup state and validate bootstrap readiness from the frontend.
Chosen fix: Add frontend client methods for local-setup and bootstrap routes, extend the onboarding stage card with real forms and actions for node identity, Core connection, bootstrap testing, and advertisement validation, and refresh persisted status after successful actions.
What changed: Added local-setup/bootstrap API helpers, wired app refresh support into the onboarding shell, added interactive forms for node identity and Core connection, added bootstrap test and retained advertisement validation controls, and extended shared component CSS with form and action styles.
Verification: `cd frontend && npm install && npm run build` (passed).
###########
Task #015
Step objective: Implement the registration, approval, and trust activation steps with real controls and operator-visible recovery context.
Chosen fix: Add frontend client methods for session start, finalize polling, and trust activation finalize, then extend the onboarding stage-card renderer to expose those actions along with session metadata and terminal outcome messaging.
What changed: Added Phase 1 action API helpers, wired registration/approval/trust-activation actions into the onboarding panel, surfaced approval/session metadata and recovery messaging for terminal outcomes, and added a neutral callout tone for status-first stage messaging.
Verification: `cd frontend && npm install && npm run build` (passed).
###########
Task #016
Step objective: Complete the onboarding UI by wiring the post-trust setup steps through provider setup, capability declaration, governance sync, and final readiness polling.
Chosen fix: Add frontend client methods for the Phase 2 routes and extend the onboarding stage-card renderer with provider-selection controls, capability declaration actions, governance fetch/refresh actions, and operational-status polling.
What changed: Added Phase 2 frontend API helpers, persisted provider-selection draft state in the onboarding panel, rendered step-aware controls for provider setup and capability declaration, surfaced governance bundle metadata and refresh actions, added ready-state operational polling, and extended shared CSS for selectable provider cards.
Verification: `cd frontend && npm install && npm run build` (passed).
###########
Task #017
Step objective: Turn the placeholder right-side cards into a real post-setup operational overview that matches the node UI standard instead of generic status blocks.
Chosen fix: Fetch provider, capability, governance, and operational status data in the app shell and project it through dedicated operational, provider, and diagnostics overview panels using facts grids, state-grid rows, grouped actions, and warning-card treatment.
What changed: Expanded the app shell data loading to include the Phase 2 overview APIs, rebuilt the operational/provider/diagnostics panels around structured state presentation, added warning-card and state-grid CSS patterns, and added a global refresh action to diagnostics.
Verification: `cd frontend && npm install && npm run build` (passed).
###########
Task #018
Step objective: Close Phase 0 with end-to-end validation evidence and operator-facing docs that match the implemented flow and UI standards.
Chosen fix: Update the repo README/setup/operations docs to reflect the real 10-step onboarding and operator shell, then run the full backend test suite plus a fresh frontend install/build as final validation.
What changed: Updated root and docs markdown to describe the implemented backend/frontend flow, documented the repo-local verification commands and operator progression, and re-ran the full backend and frontend validation commands.
Verification: `PYTHONPATH=src .venv/bin/pytest -q` (33 passed); `cd frontend && npm install && npm run build` (passed).
###########
Task #028
Step objective: Define the backend-owned voice event envelope and MVP single-endpoint session contract without introducing audio processing or a transport route.
Chosen fix: Add a dedicated `hexevoice.voice` contract module with Pydantic event/session payload models, explicit event vocabularies, separate endpoint connection/UX/backend session states, and allowed session transition validation.
What changed: Added voice contract exports, payload models for session start/audio chunks/transcripts/responses/TTS/errors, a shared `VoiceEventEnvelope`, a `VoiceSessionSnapshot`, transition validation tests, and documentation updates that mark the contract as real while keeping `/api/voice/ws`, wake, STT, TTS, and runtime session management pending.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_contracts.py` (6 passed); `PYTHONPATH=src .venv/bin/pytest -q` (48 passed, 30 FastAPI deprecation warnings).
###########
Task #029
Step objective: Add the first `/api/voice/ws` runtime path using the Task 028 envelope while keeping the manager in memory and single-endpoint for the MVP.
Chosen fix: Add `VoiceSessionManager`, register `/api/voice/ws`, bind one connected endpoint/session at a time, accept session/control/audio chunk metadata events, and respond with state, completion, cancellation, and error envelopes through the same contract.
What changed: Added WebSocket session handling, JSON/envelope validation, active-session checks, audio chunk metadata validation, cancel and completion handling, focused WebSocket tests, and docs that mark the route as implemented but wake/STT/TTS/audio processing as pending.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_contracts.py tests/test_voice_websocket.py` (12 passed); `PYTHONPATH=src .venv/bin/pytest -q` (54 passed, 42 FastAPI deprecation warnings).
###########
Task #030
Step objective: Add a backend-owned wake intake path so WebSocket audio chunks are inspected by a wake detector adapter before any STT/TTS wiring exists.
Chosen fix: Add `WakeDetector` plus deterministic and openWakeWord adapter implementations, inject the detector into `VoiceSessionManager`, feed `audio.chunk` payloads through the detector, and emit `wake.accepted` plus session state envelopes when backend wake detection fires.
What changed: Added `src/hexevoice/voice/wake.py`, updated the WebSocket session lifecycle to start in `wake_armed`, added no-wake cancellation on `audio.end`, added deterministic wake tests, and updated docs to keep firmware VAD as optional while naming backend openWakeWord as the canonical wake authority.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_contracts.py tests/test_voice_websocket.py tests/test_voice_wake.py` (15 passed); `PYTHONPATH=src .venv/bin/pytest -q` (57 passed, 44 FastAPI deprecation warnings).
###########
Task #031
Step objective: Wire firmware endpoint configuration and transport so the ESP32 can send heartbeat metadata and bounded microphone chunks to the backend without hardcoding the node IP.
Chosen fix: Generate firmware endpoint constants from `firmware/config/endpoint.yaml` or the committed example during the ESP-IDF build, add a native backend client with heartbeat and voice WebSocket tasks, and enqueue microphone frames from the existing VAD loop through a bounded queue.
What changed: Added the endpoint config generator and test, added `firmware/main/voice/backend_client.*`, wired the client into boot and audio capture, extended CMake component requirements, and updated firmware docs/baseline notes. Firmware still does not consume backend events or play TTS.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_firmware_endpoint_config.py` (1 passed); `PYTHONPATH=src .venv/bin/pytest -q` (58 passed, 44 FastAPI deprecation warnings). `idf.py build` could not run because `idf.py` is not installed in this shell.
###########
Task #032
Step objective: Add backend STT and TTS adapter boundaries and route completed wake sessions through the existing assistant turn service.
Chosen fix: Add `VoiceTurnPipeline` with STT/TTS protocols and deterministic adapters, inject it into the WebSocket session manager, and emit transcript, assistant response, TTS metadata, and completion envelopes after `audio.end`.
What changed: Added `src/hexevoice/voice/pipeline.py`, exported provider adapter boundaries, wired the pipeline into `create_app`, extended WebSocket turn completion events, added pipeline/WebSocket tests, and updated docs to distinguish deterministic provider adapters from real STT/TTS implementations.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_pipeline.py tests/test_voice_websocket.py tests/test_voice_contracts.py` (15 passed); `PYTHONPATH=src .venv/bin/pytest -q` (60 passed, 46 FastAPI deprecation warnings).
###########
Task #033
Step objective: Let firmware react to backend voice events and add the first playback/control boundary while preserving existing display and app-state conventions.
Chosen fix: Parse backend WebSocket event envelopes in the firmware backend client, map wake/transcript/response/TTS/completion/error events to app phases, add a TTS-ready playback scaffold, send `audio.end` on VAD silence, and send `session.cancel` from mute/config controls where practical.
What changed: Added backend event parsing with cJSON, fixed the speaking state mapping to use existing `kReplying`, added TTS-ready/stop hooks, wired VAD silence to finish the audio stream, wired mute/config buttons to cancel active sessions, and updated firmware docs. Real TTS audio download/playback remains pending.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_firmware_endpoint_config.py` (1 passed); `PYTHONPATH=src .venv/bin/pytest -q` (60 passed, 46 FastAPI deprecation warnings). `idf.py build` could not run because `idf.py` is not installed in this shell.
###########
Task #034
Step objective: Replace voice dashboard placeholders with live endpoint/session observability and supported operator actions.
Chosen fix: Add backend voice status and cancel APIs backed by the in-memory session manager, fetch voice status in the app shell, and rebuild the voice endpoint dashboard around connection, transport, active session, latest transcript/response/TTS/error, and action support.
What changed: Added `/api/voice/status` and `/api/voice/session/cancel`, tracked recent voice metadata in `VoiceSessionManager`, added frontend API helpers, replaced placeholder voice cards with live observability cards, wired refresh/test assistant/stop session actions, and documented the remaining replay/mute/reconnect gaps.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_websocket.py` (9 passed); `PYTHONPATH=src .venv/bin/pytest -q` (61 passed, 48 FastAPI deprecation warnings); `cd frontend && npm run build` (passed).
###########
Task #035
Step objective: Validate and document the first integrated single-endpoint wake-to-reply loop across backend contracts, firmware paths, and frontend observability.
Chosen fix: Add a named integration test for endpoint heartbeat, WebSocket session start, wake acceptance, transcript finalization, assistant response, TTS metadata, completion, dashboard status, and cancel behavior, then document the implementation and remaining provider/device gaps in a handoff note.
What changed: Added `tests/test_voice_loop_integration.py`, created `docs/voice-loop-phase-1-handoff.md`, updated phase docs to reflect the tested MVP loop, and recorded that ESP-IDF build validation is blocked in this shell because `idf.py` is unavailable.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_loop_integration.py` (2 passed); `PYTHONPATH=src .venv/bin/pytest -q` (63 passed, 52 FastAPI deprecation warnings); `cd frontend && npm run build` (passed); `cd firmware && idf.py build` blocked because `idf.py` is not installed.
###########
Task #059
Step objective: Persist endpoint heartbeat records and expose operator metadata editing.
Chosen fix: Add a dedicated endpoint registry store under `runtime/endpoint_registry.json`, merge heartbeat runtime fields without overwriting operator display name or zone, and expose registry list/update APIs consumed by the voice endpoint dashboard.
What changed: Added endpoint registry persistence, heartbeat upsert/status/list/update behavior, capability and stale-state projection, dashboard display-name/zone editing, focused backend tests, and updated Phase 1 docs.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_endpoint_registry.py tests/test_api.py tests/test_voice_loop_integration.py` (29 passed); `cd frontend && npm run build` (passed); `PYTHONPATH=src .venv/bin/pytest -q` (106 passed).
###########
Task #060
Step objective: Formalize connection, UX, and session state separation.
Chosen fix: Centralize voice state projection in `src/hexevoice/voice/contracts.py`, map session states to UX states in one helper, expose the projection through `/api/voice/status`, and update frontend/firmware display paths to read explicit state families.
What changed: Replaced old routing/waiting/synthesizing/playing states with `routing` and `responding`, added state projection tests, updated session manager transitions/status payloads, refreshed dashboard state-family rendering, mapped firmware thinking display from `ux_state`, and updated phase docs.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_contracts.py tests/test_voice_websocket.py tests/test_voice_loop_integration.py` (23 passed); `PYTHONPATH=src .venv/bin/pytest -q` (108 passed); `cd frontend && npm run build` (passed); `cd firmware && idf.py build` blocked because `idf.py` is not installed.
###########
Task #061
Step objective: Version and validate the endpoint event envelope.
Chosen fix: Add `event_id` and `schema_version` defaults to `VoiceEventEnvelope` for backward compatibility, reject unknown schema versions through normal envelope validation, add structured endpoint `command.ack` and `command.error` events, and preserve all JSON schema/example artifacts under `docs/voice-event-envelope/`.
What changed: Added command acknowledgement/error payload models, event diagnostics in `/api/voice/status`, command ack/error handlers, backend command request ids, firmware warnings for malformed or unknown backend events, envelope contract docs, JSON schema/examples, and tests for accepted/rejected shapes and documented JSON artifacts.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_contracts.py tests/test_voice_websocket.py tests/test_voice_loop_integration.py tests/test_api.py` (56 passed); `PYTHONPATH=src .venv/bin/pytest -q` (118 passed); `cd frontend && npm run build` (passed); JSON validation for 5 files under `docs/voice-event-envelope/` passed; `cd firmware && idf.py build` blocked because `idf.py` is not installed.
###########
Task #062
Step objective: Complete endpoint command APIs and dashboard controls.
Chosen fix: Reuse the versioned voice envelope for backend-issued endpoint commands, track command request ids and terminal lifecycle state in the session manager, expose focused API models/routes, and wire the existing dashboard controls to those APIs.
What changed: Added volume status, mute/unmute, cancel, and replay endpoint APIs; added command tracking with pending/succeeded/failed/timed_out/unsupported status; updated firmware command handling and explicit unsupported-command errors; wired dashboard controls and recent command status display; removed the standalone Phase n markdown files and made `docs/voice-node-roadmap.md` the current roadmap source.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_api.py tests/test_voice_websocket.py tests/test_voice_contracts.py tests/test_voice_loop_integration.py` (58 passed); `PYTHONPATH=src .venv/bin/pytest -q` (120 passed); `cd frontend && npm run build` (passed); `cd firmware && idf.py build` blocked because `idf.py` is not installed.
###########
Task #063
Step objective: Add firmware persistent settings and capability reporting.
Chosen fix: Move endpoint volume/mute ownership into the firmware settings layer backed by NVS, reuse those setters from backend commands and local buttons, and include board/runtime capabilities in each heartbeat so the existing registry can persist them.
What changed: Added NVS load/save for volume and mute, initialized settings after NVS storage, persisted backend and button mute/volume changes, added display capability accessors, expanded firmware heartbeat capabilities for touchscreen, SD card, display, audio I/O, controls, and firmware build metadata, and displayed capabilities in the dashboard.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_endpoint_registry.py tests/test_api.py` (29 passed); `PYTHONPATH=src .venv/bin/pytest -q` (120 passed); `cd frontend && npm run build` (passed); `cd firmware && idf.py build` blocked because `idf.py` is not installed.
###########
#############
All Tasks completed 04/26/2026 06:11:26
#############
###########
Task #077
Step objective: Define endpoint SD media delivery contract.
Chosen fix: Added a focused contract document covering destinations, transfer envelope, result events, validation rules, and initial size limits.
What changed: Created `docs/endpoint-sd-media-contract.md` and completed Task 077 in the task queue.
Verification: Contract formatting checked by direct file read; no runtime tests required for documentation-only contract.
###########
Task #078
Step objective: Add backend media upload and endpoint delivery APIs.
Chosen fix: Added a JSON/base64 endpoint media service so media can be staged, validated, served, and delivered without adding multipart runtime dependencies.
What changed: Added endpoint media storage, validation, file serving, transfer command routing, API models, settings, and API tests.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_api.py -q` passed.
###########
Task #079
Step objective: Add firmware media-transfer command handling.
Chosen fix: Added a queued firmware worker for `endpoint.media.transfer` so downloads, SD writes, checksum verification, and final rename do not run in the WebSocket event handler.
What changed: Firmware now accepts media transfer commands, validates destination/filename, downloads endpoint media payloads, writes temporary files, verifies SHA-256/size, renames into place, and reports command ack/error state.
Verification: `./firmware/build.sh` passed.
###########
Task #080
Step objective: Add endpoint SD media inventory reporting.
Chosen fix: Reused heartbeat capabilities as the persistence path and added a focused read API for the latest endpoint SD inventory.
What changed: Firmware now scans pictures, sprites, and sounds directories into bounded heartbeat inventory arrays; the backend exposes `GET /api/endpoint/media/inventory/{endpoint_id}` from persisted endpoint capabilities.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_api.py::test_endpoint_media_inventory_projects_heartbeat_storage_inventory tests/test_api.py::test_standard_route_groups_exist -q` passed; `./firmware/build.sh` passed.
###########
Task #081
Step objective: Add sprite/item asset support under `/sdcard/hexe/sprites`.
Chosen fix: Added a simple overlay manifest convention that can be produced by tooling and consumed by firmware without adding a UI framework dependency.
What changed: Firmware now reads `/sdcard/hexe/sprites/overlay.json`, validates sprite geometry, loads the referenced RGB565 sprite, and draws it over non-boot UI screens; added `firmware/tools/convert-sprite.sh` to create sprite payloads and manifests.
Verification: `bash -n firmware/tools/convert-sprite.sh` passed; `./firmware/build.sh` passed.
###########
Task #082
Step objective: Add SD sound asset delivery and playback integration.
Chosen fix: Reused the existing WAV PCM playback worker and added a local SD file source so delivered sound assets can be played without another audio path.
What changed: Firmware can queue `/sdcard/hexe/sounds/*.wav` playback through `play_sd_sound`; completed sound transfers with `activate: true` trigger local playback after checksum verification and rename.
Verification: `./firmware/build.sh` passed.
###########
Task #083
Step objective: Add dashboard media manager for endpoint SD assets.
Chosen fix: Added media upload, staged asset actions, delivery, and latest endpoint inventory to the existing voice endpoint dashboard.
What changed: Frontend API helpers now cover media list/upload/delete/deliver/inventory; the dashboard can stage files, deliver selected assets to the endpoint, and show reported SD pictures, sprites, and sounds.
Verification: `cd frontend && npm run build` passed.
###########
Task #084
Step objective: Add media transfer validation and recovery tests.
Chosen fix: Added focused API tests for rejected uploads, duplicate protection, delete cleanup, and disconnected delivery, plus firmware source guards for temp-file/checksum recovery behavior.
What changed: Expanded media API test coverage and firmware transfer recovery assertions.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_api.py::test_endpoint_media_upload_validates_and_serves_picture_rgb565 tests/test_api.py::test_endpoint_media_upload_rejects_invalid_base64_and_duplicate_asset tests/test_api.py::test_endpoint_media_upload_rejects_sprite_without_dimensions tests/test_api.py::test_endpoint_media_delete_removes_staged_payload_and_listing tests/test_api.py::test_endpoint_media_deliver_reports_disconnected_endpoint tests/test_firmware_voice_envelope.py -q` passed.
#############
All Tasks completed 04/27/2026 00:45:18
#############
###########
Task #085
Step objective: Define composited endpoint UI asset model and SD manifest.
Chosen fix: Added a manifest-driven UI composition contract that keeps backgrounds in pictures and avatars/sprites/manifests in sprites.
What changed: Created `docs/endpoint-ui-composition.md`, linked it from the SD media contract, and completed Task 085 in the task queue.
Verification: Documentation reviewed by direct file read; no runtime verification required for the contract-only step.
###########
Task #086
Step objective: Refactor firmware display renderer around layered scene composition.
Chosen fix: Load `/sdcard/hexe/sprites/ui_manifest.json` into a composited scene with background, avatar, dynamic overlay, and sprite layers while preserving the legacy fullscreen and simple-drawing fallback paths.
What changed: Added layer asset loading, RGB565 alpha blending, manifest parsing, scene rendering, and compatibility behavior for the older full-screen SD images and overlay manifest.
Verification: `./firmware/build.sh` passed.
###########
Task #087
Step objective: Add alpha-capable avatar asset format and conversion support.
Chosen fix: Support `alpha8`, `alpha1`, and color-key transparency in firmware scene layers, and allow staged `.alpha8`/`.alpha1` sprite payloads through the backend media manager.
What changed: Firmware now blends alpha-mask avatars/sprites; backend upload validation accepts alpha masks; dashboard exposes alpha-mask asset class metadata.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_api.py tests/test_image_converter.py tests/test_firmware_voice_envelope.py -q` passed.
###########
Task #088
Step objective: Map endpoint states to avatar variants loaded from SD.
Chosen fix: Reuse the existing app phase projection and map each phase to a manifest avatar key, falling back to `idle` if a specific variant is missing.
What changed: Added manifest avatar keys for logo, idle, listening, work, thinking, talk, and error, and rendered them through the composed scene path.
Verification: `./firmware/build.sh` passed; firmware source coverage in `tests/test_firmware_voice_envelope.py` passed.
###########
Task #089
Step objective: Add general avatar scene type support such as clock scenes.
Chosen fix: Treat `type: "clock"` or a `clock` object in the manifest as a dynamic scene overlay rendered between avatar and sprite layers.
What changed: Added clock hand and date drawing support with configurable center, radius, color, and date visibility.
Verification: `./firmware/build.sh` passed; firmware source coverage in `tests/test_firmware_voice_envelope.py` passed.
###########
Task #090
Step objective: Expand sprite support for buttons, icons, and touch targets.
Chosen fix: Add a bounded sprite array to the scene manifest so buttons/icons can be loaded and drawn now, with touch metadata preserved in the manifest contract for the future interaction layer.
What changed: Firmware loads and renders up to eight manifest sprites after the avatar/dynamic layers; docs define sprite touch metadata without activating touch behavior yet.
Verification: `./firmware/build.sh` passed.
###########
Task #091
Step objective: Update backend and dashboard media manager for UI asset classes and media-only SD reformat.
Chosen fix: Extend existing endpoint command/media APIs rather than adding a separate channel, using `endpoint.storage.reformat` for media-only folder cleanup and metadata-driven UI asset classes for staged media.
What changed: Added backend route and session-manager command for SD media reformat, firmware command handling, alpha-mask staging, dashboard asset class and sprite geometry controls, and a reformat button.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_api.py tests/test_image_converter.py tests/test_firmware_voice_envelope.py -q` passed; `cd frontend && npm run build` passed.
###########
Task #092
Step objective: Add UI composition tests, docs, and migration path.
Chosen fix: Document the manifest, alpha formats, layer order, state mapping, and media-only reformat behavior; add tests that pin the backend API and firmware source contracts.
What changed: Updated SD media and UI composition docs, added API tests for alpha masks and reformat command delivery, and added firmware source checks for composition and reformat behavior.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_api.py tests/test_image_converter.py tests/test_firmware_voice_envelope.py -q` passed.
#############
All Tasks completed 04/27/2026 01:25:00
#############
###########
Task #117
Step objective: Move external faster-whisper STT service into src/stt package boundary.
Chosen fix: Created a standalone stt package for the FastAPI runtime and adapter exports while preserving the old hexevoice.stt_service module as a compatibility shim.
What changed: Added src/stt/service.py and src/stt/adapters.py, replaced hexevoice.stt_service with a wrapper, updated STT service tests to target the new package, and documented the package boundary.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_stt_service.py tests/test_voice_pipeline.py::test_faster_whisper_stt_adapter_transcribes_temp_wav_and_removes_it tests/test_voice_pipeline.py::test_faster_whisper_stt_adapter_preloads_model` passed with 5 tests.
###########
Task #118
Step objective: Update STT service launch, tests, and docs after the src/stt package split.
Chosen fix: Pointed local stack launch configuration at `python -m stt.service` while preserving the existing systemd/control-script contract.
What changed: Updated `scripts/stack.env` and `scripts/stack.env.example`, added an operations migration note for existing user units, and completed the task queue entry.
Verification: `bash -n scripts/faster-whisper-stt-control.sh scripts/run-from-env.sh scripts/stack-control.sh scripts/restart-stack.sh` passed; `PYTHONPATH=src .venv/bin/python` import check confirmed the compatibility entrypoint still wraps `stt.service`; `PYTHONPATH=src .venv/bin/pytest -q tests/test_stt_service.py tests/test_supervisor_runtime.py::test_external_stt_service_status_action_and_supervisor_metadata` passed with 4 tests.
###########
Task #119
Step objective: Move the Piper TTS service into a dedicated src/tts package boundary.
Chosen fix: Moved the Piper FastAPI runtime into `src/tts/service.py` and kept `services/piper_tts/app.py` as a thin compatibility wrapper.
What changed: Added the `tts` package, moved the service implementation, updated direct service tests to import `tts.service`, and documented the new TTS package boundary.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_piper_tts_service.py` passed with 3 tests; `PYTHONPATH=src .venv/bin/python` import check confirmed the Docker compatibility wrapper exposes the same FastAPI app.
###########
Task #120
Step objective: Update TTS service launch, tests, and docs after the src/tts package split.
Chosen fix: Updated the Piper image to copy the standalone `src/tts` package and launch `tts.service:app` directly.
What changed: Updated `services/piper_tts/Dockerfile`, added a Dockerfile launch-path regression test, documented the image rebuild/restart migration step, and completed the task queue entry.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_piper_tts_service.py tests/test_supervisor_runtime.py::test_supervisor_runtime_registration_includes_piper_tts_when_configured tests/test_supervisor_runtime.py::test_piper_tts_service_action_uses_control_script_when_enabled tests/test_supervisor_runtime.py::test_tts_service_action_alias_restarts_piper_when_enabled` passed with 7 tests; `./scripts/piper-tts-control.sh config` rendered successfully.
#############
All Tasks completed 05/09/2026 22:33:44
#############
###########
Task #093
Step objective: Close the completed faster-whisper model benchmark task.
What changed: Removed the future TODO after confirming local benchmark artifacts for `base.en`, `small.en`, and `medium.en` exist under `runtime/stt/`. The five-repeat REHASp sample benchmark showed `base.en` averaging 1523.53 ms with 1/2 normalized matches, `small.en` averaging 4519.99 ms with 2/2 matches, and `medium.en` averaging 14755.03 ms with 2/2 matches.
Verification: Inspected `runtime/stt/faster-whisper-base-en-rehasp-repeat5.json`, `runtime/stt/faster-whisper-small-en-rehasp-repeat5.json`, and `runtime/stt/faster-whisper-medium-en-rehasp-repeat5.json`.
###########
Task #095
Step objective: Add faster-whisper STT decode tuning options.
What changed: Added settings/env support for language, beam size, best-of, without-timestamps, word timestamps, and max initial timestamp; wired those options into local and external STT service adapter construction; exposed them in adapter status; documented the environment knobs.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_config.py::test_faster_whisper_stt_settings_defaults tests/test_stt_service.py tests/test_voice_pipeline.py::test_faster_whisper_stt_adapter_transcribes_temp_wav_and_removes_it tests/test_voice_pipeline.py::test_faster_whisper_stt_adapter_preloads_model tests/test_voice_pipeline.py::test_build_voice_turn_pipeline_uses_faster_whisper_stt_when_configured` passed with 7 tests.
###########
Task #097
Step objective: Add STT timing breakdown metrics.
What changed: Added `timing_breakdown_ms` to STT transcript results. Local faster-whisper now records audio preparation, model transcribe call, segment decoding, post-processing, and total timing; the external STT service returns the same block and the backend preserves it.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_stt_service.py tests/test_voice_pipeline.py::test_faster_whisper_stt_adapter_transcribes_temp_wav_and_removes_it tests/test_voice_pipeline.py::test_external_faster_whisper_stt_adapter_posts_audio_to_service tests/test_voice_pipeline.py::test_external_faster_whisper_stt_status_clears_stale_connection_error` passed with 6 tests.
###########
Task #096
Step objective: Surface STT warm-model health and reload detection.
What changed: Local faster-whisper status now includes load count, loaded-at timestamp, loaded config, and reload-required state. Backend runtime/service metadata fetches external STT health, exposes warm model load metadata, and marks reload required when the running STT service config differs from backend expectations.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_pipeline.py::test_faster_whisper_stt_adapter_preloads_model tests/test_stt_service.py tests/test_supervisor_runtime.py::test_external_stt_service_status_action_and_supervisor_metadata tests/test_supervisor_runtime.py::test_external_stt_runtime_metadata_marks_reload_required_on_config_mismatch` passed with 6 tests.
###########
Task #206
Step objective: Upgrade the frontend dev toolchain to a supported Vite security baseline.
Issue cause: The current Vite 5/esbuild chain could not clear npm audit findings without moving to the Vite 8 line, which requires Node.js `^20.19.0 || >=22.12.0`.
Chosen fix: Make the new Node range an explicit frontend engine contract, enforce it in npm and repo bootstrap scripts, and have Debian/Ubuntu installer guidance install NodeSource 22.x.
What changed: Updated Vite/plugin-react and the lockfile, added frontend `engine-strict`, added installer/rebuild Node version checks, documented the baseline in README/setup docs, and updated local dependency install hints.
Verification: With Node v22.12.0 from `/tmp/node-v22.12.0-linux-x64`, `npm install`, `npm run build`, `npm audit`, `./scripts/rebuild-ui.sh`, and a Vite dev-server curl smoke test passed; `bash -n install.sh scripts/rebuild-ui.sh scripts/start-dev-stack.sh` passed.
###########
Task #207
Step objective: Migrate FastAPI startup/shutdown hooks to lifespan handlers.
Issue cause: `@app.on_event("startup")` and `@app.on_event("shutdown")` are deprecated and were still used by the backend, external STT service, and Piper TTS service.
Chosen fix: Replace deprecated event decorators with `lifespan=` context managers while preserving warmup, scheduler, and cleanup behavior.
What changed: Backend startup tasks now start inside lifespan and are tracked for deterministic shutdown; STT preload runs inside its app lifespan; Piper warm workers start and stop from the service lifespan.
Verification: `rg -n "on_event\\(" src tests` found no remaining calls; compile checks passed for the three touched modules; `tests/test_stt_service.py`, `tests/test_piper_tts_service.py`, `tests/test_api.py`, `tests/test_node_ui_pilot.py`, two voice websocket restart tests, and `PYTHONWARNINGS='error::DeprecationWarning'` import smoke passed; full backend suite passed with 418 tests.
###########
Task #208
Step objective: Add a one-command repo move-readiness preflight.
Issue cause: Fresh repo validation required several manual commands across Python dependency setup, backend tests, frontend build, and npm audits.
Chosen fix: Add `scripts/repo-migration-preflight.sh` as the documented canonical preflight, with required checks separated from non-blocking dev-audit review.
What changed: The script prepares Python/frontend dependencies by default, reports Python/Node/npm/Vite tool versions, runs backend pytest, frontend build, production `npm audit --omit=dev`, and full dev `npm audit`, with logs under `runtime/preflight/`; README and setup docs now point maintainers to the command.
Verification: `bash -n scripts/repo-migration-preflight.sh` passed; `tests/test_repo_migration_preflight_script.py` passed; with Node v22.12.0 from `/tmp/node-v22.12.0-linux-x64`, `./scripts/repo-migration-preflight.sh` passed with backend suite, frontend build, production audit, and full dev audit clean, logging to `runtime/preflight/repo-migration-20260823T041320Z`.
###########
Task #209
Step objective: Preserve legacy wake-model compatibility while removing the misspelled path as a normal runtime dependency.
Issue cause: `scripts/openwakeword-control.sh` still directly depended on `runtime/vioce_models/Hexa.tflite` as a fallback, and `.gitignore` explicitly listed legacy wake model directories.
Chosen fix: Convert the misspelled local directory into a one-time migration source that moves wake model files into `runtime/openwakeword/models/` and removes the old directory when empty.
What changed: `sync-models` migrates `.tflite`/`.onnx` files from the legacy local directory, preserves conflicting model files with `.legacy-N` names, keeps the old Hexa-to-Hexe compatibility copy after migration, defaults the control script to the repo venv Python when available, removes explicit legacy ignore entries, and documents the compatibility behavior.
Verification: `bash -n scripts/openwakeword-control.sh` passed; `tests/test_openwakeword_control_script.py` passed; direct legacy migration smoke moved `Hexa.tflite` into a canonical temp model dir and removed the temp misspelled dir; `tests/test_post_install_smoke_test.py::test_post_install_smoke_test_passes_with_healthy_stack` passed.
###########
Task #210
Step objective: Make firmware hardware-profile validation explicit and test-enforced.
Issue cause: Supported firmware profiles existed in build/test code, but release readiness across audio streaming, wake, TTS playback, display/LED state, OTA/media, mute/volume, and reconnect behavior was not captured as an operator-visible matrix.
Chosen fix: Add a machine-readable firmware validation matrix plus human docs and tests that keep the matrix aligned with supported build profiles and required categories.
What changed: Added `docs/firmware-validation-matrix.json`, `docs/firmware-validation-matrix.md`, linked it from firmware docs, and added matrix tests that require every supported profile to declare automated coverage and manual field checks for each required validation category.
Verification: `tests/test_firmware_validation_matrix.py`, `tests/test_firmware_voice_envelope.py`, `tests/test_voice_loop_integration.py`, and `tests/test_voice_websocket.py` passed; `bash -n firmware/build.sh firmware/export-artifacts.sh` passed. `idf.py` is not installed in this shell, so a real ESP-IDF firmware build was not run.
###########
Task #211
Step objective: Add staging-friendly long-running provider/model/media lifecycle validation.
Issue cause: Provider health, warmup, cleanup, firmware manifest, and media lifecycle checks were split across smoke tests and manual inspection rather than available as a repeated validation command.
Chosen fix: Add a stdlib-only lifecycle validator that runs repeated cycles against a local or staging backend, separates warnings from required failures, and supports JSON output for setup/ops automation.
What changed: Added `scripts/provider-lifecycle-validation.py` with provider health, warm-model, TTS warmup, cleanup, control-script, runtime artifact directory, endpoint media API, and firmware manifest checks; documented the staging command in setup docs; added fake-server tests for repeated success and required failure behavior.
Verification: `python3 -m py_compile scripts/provider-lifecycle-validation.py` passed; `tests/test_provider_lifecycle_validation_script.py`, `tests/test_post_complete_lifecycle_script.py`, `tests/test_post_install_smoke_test.py`, and `tests/test_api.py::test_voice_status_reports_tts_warmup_background_task` passed.
#############
All Tasks completed 08/22/2026 21:23:49
#############
###########
Task #212
Step objective: Reconcile stale Voice Node documentation and retain the feature report intentionally.
Issue cause: The node feature report was untracked, and older architecture/firmware docs still described implemented backend voice and firmware TTS paths as pending or missing.
Chosen fix: Commit the feature report as an audit artifact, add cross-links to canonical docs and follow-up tasks, and narrow stale wording in the canonical architecture and firmware baseline docs.
What changed: Updated `docs/architecture.md` to reflect implemented STT/assistant/TTS/session-history backend paths, updated `docs/firmware-baseline.md` to distinguish implemented TTS playback from remaining device validation/integrity hardening, and retained `docs/node-feature-report.md` with follow-up task links.
Verification: Text checks confirmed the stale pending/missing phrases no longer appear in current docs except historical `docs/Reasoning.txt`; cross-link checks found the new report and canonical-doc references.
###########
Task #213
Step objective: Add firmware provisioning and endpoint settings UI flow.
Issue cause: Endpoint identity, backend host/ports, TLS mode, and Wi-Fi credentials were still effectively build-time/local-file settings, leaving normal endpoint provisioning dependent on source edits or local secrets.
Chosen fix: Extend the existing firmware settings module as the NVS-backed source of active endpoint provisioning, route Wi-Fi/backend/TTS runtime consumers through it, and expose apply/reset commands through the existing WebSocket command path and operator dashboard.
What changed: Firmware now persists endpoint id, display name, backend host, HTTP/WebSocket ports, TLS mode, and optional Wi-Fi credentials with generated YAML/secrets fallback; heartbeat capabilities report provisioning state; backend API routes and VoiceSessionManager can send provisioning apply/reset commands; the dashboard includes an Endpoint Settings provisioning panel; docs capture the contract and recovery behavior.
Verification: Targeted provisioning tests passed; firmware voice-envelope tests passed; endpoint command WebSocket tests passed; voice contract/API route checks passed; frontend Vite build passed with Node v22.21.1; `./firmware/build.sh` built both `esp_box_3` and `ha_voice_pe` profiles successfully.
###########
Task #214
Step objective: Add automatic endpoint discovery and pairing for firmware endpoints.
Issue cause: Firmware still needed a static backend host/port from endpoint YAML unless an operator manually provisioned runtime settings.
Chosen fix: Add a LAN UDP discovery offer flow backed by the endpoint registry, with duplicate-online rejection, stale pairing recovery, HTTP simulation fallback, and firmware persistence through the existing provisioning settings layer.
What changed: Added endpoint discovery settings, API models, backend discovery service, UDP listener, and HTTP offer route; generated firmware discovery config now includes an enabled flag and UDP port; firmware broadcasts discovery after Wi-Fi, persists accepted offers, reports discovery status in heartbeat capabilities, and initializes the voice WebSocket after discovered settings are active; dashboard and docs show discovery state and recovery paths.
Verification: `tests/test_endpoint_discovery.py`, firmware endpoint config/source tests, endpoint registry/API checks, frontend Vite build with Node v22.21.1, and `./firmware/build.sh` for both `esp_box_3` and `ha_voice_pe` passed.
###########
Task #215
Step objective: Harden firmware OTA integrity with checksum enforcement and signed manifest validation.
Issue cause: The backend sent OTA URL, version, size, and checksum metadata, but firmware accepted unsigned metadata and did not compare the downloaded image bytes to the advertised SHA-256 before finishing OTA.
Chosen fix: Sign the exact pushed OTA metadata with HMAC-SHA256, generate matching endpoint trust constants, validate profile/version/signature/checksum metadata before download, and hash OTA stream bytes before `esp_https_ota_finish()`.
What changed: Firmware OTA now rejects missing/invalid signatures, unknown signing keys, wrong board profiles, same/lower versions, malformed checksums, size mismatches, and checksum mismatches; backend manifest/status/push APIs include signed metadata; endpoint capabilities and discovery expose board profile and OTA integrity policy; docs define the trust model and failure codes.
Verification: `tests/test_api.py`, `tests/test_voice_websocket.py`, firmware endpoint config/source tests, and `./firmware/build.sh` for both `esp_box_3` and `ha_voice_pe` passed.
###########
Task #216
Step objective: Validate physical-device reconnect and session-boundary behavior across supported profiles.
Issue cause: Reconnect/session behavior had automated backend coverage and manual matrix notes, but no repeatable field procedure or release artifact for physical endpoint evidence.
Chosen fix: Add a stdlib field-validation runner that records pass/fail/blocked results per profile and scenario, seed a structured release-review artifact, and link the firmware validation matrix to the procedure and results.
What changed: Added `scripts/firmware-reconnect-session-validation.py`, `docs/firmware-reconnect-session-validation.md`, and `docs/firmware-reconnect-session-results.json`; updated the validation matrix JSON/docs; added tests for an all-pass simulated bench run and the blocked seed artifact. Because no physical endpoints were attached, the seed artifact is intentionally `blocked` and Task 218 tracks the bench run that must replace it with operator pass/fail evidence.
Verification: `tests/test_firmware_reconnect_session_validation.py`, `tests/test_firmware_validation_matrix.py`, and `python3 -m py_compile scripts/firmware-reconnect-session-validation.py` passed.
###########
Task #217
Step objective: Replace or explicitly retire scaffold firmware modules for wake word, STT stream, assistant client, telemetry, and power.
Issue cause: Five firmware modules were still source-level scaffolds that only logged readiness, which made ownership unclear even though backend/session code already owns wake, STT, assistant turns, telemetry projection, and most power behavior.
Chosen fix: Retain the modules as intentional no-op/status providers with explicit mode, owner, and local-availability accessors; expose those decisions through heartbeat capabilities under `firmware.modules`.
What changed: Wake, STT, assistant, telemetry, and power modules now report backend/heartbeat/board ownership instead of scaffold readiness; boot and animator logs no longer describe scaffolds; heartbeat capabilities expose module state; firmware baseline and feature report docs describe each retained intentional no-op.
Verification: `tests/test_firmware_voice_envelope.py` passed and `./firmware/build.sh` built both `esp_box_3` and `ha_voice_pe` successfully.
###########
Task #218
Step objective: Add a guided one-device-at-a-time physical bench runner.
Issue cause: The first physical bench attempt needed safer operator flow: Wi-Fi loss/rejoin was intentionally skipped, and manual wake/session-boundary testing needed explicit per-scenario pass/fail/blocked recording without driving the lower-level Python runner directly.
Chosen fix: Add a shell wrapper that selects one endpoint profile, prompts for each physical scenario, waits for operator completion, collects backend observations, asks for pass/fail/blocked, and merges only that profile into the release artifact.
What changed: Added `scripts/firmware-reconnect-session-bench.sh`; added explicit skip support to the Python runner; updated the reconnect/session validation procedure; added regression coverage for interactive skip recording and the guided shell runner.
Verification: `bash -n scripts/firmware-reconnect-session-bench.sh scripts/firmware-ota-menu.sh` and `PYTHONPATH=src .venv/bin/pytest -q tests/test_firmware_reconnect_session_validation.py` passed.
###########
Operational fix
Step objective: Recover wake invocation when repeated endpoint session starts leave the UI in `session_conflict`.
Issue cause: Supervised openWakeWord can receive and count pre-wake audio while the active backend session is still `idle`. The replacement guard treated any observed chunk as an active turn, so later endpoint `session.start` events were rejected and their audio produced repeated `session_conflict` diagnostics.
Chosen fix: Allow same-endpoint `idle` sessions with no accepted wake record to be superseded, even after pre-wake chunks were observed; keep the active-session rejection once the session has accepted wake and moved into capture.
What changed: `VoiceSessionManager` now centralizes active-session replacement eligibility and distinguishes pre-wake audio from post-wake/capturing audio. WebSocket regression tests cover both replacement and rejection paths.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_websocket.py` passed.
###########
Operational fix
Step objective: Align supervised openWakeWord runtime model selection with the installed Wyoming model name.
Issue cause: The Wyoming openWakeWord service advertises the custom model as `hexe`, while `scripts/stack.env.example` and the live `scripts/stack.env` requested `VOICE_WAKE_MODELS=Hexe`. The service was reachable and the model file existed, but the backend filtered detection requests by a non-advertised model name.
Chosen fix: Set the supervised wake runtime model selector to `hexe`.
What changed: Updated `scripts/stack.env.example`; the local ignored `scripts/stack.env` was updated on this node as well.
Verification: Direct Wyoming `describe` returned model `hexe`; after backend restart `/api/voice/status` reports `models=["hexe"]` with supervised openWakeWord healthy/loaded.
###########
Task #219
Step objective: Add event-driven timer expiry playback on the endpoint that started the timer.
Issue cause: Timer create/status routing exists, but a promoted timer completion event does not yet trigger endpoint alarm playback.
Chosen fix: Reuse the existing endpoint play-sound command path and add a timer-completed subscriber that resolves `data.endpoint_id`, deduplicates promoted events, and reports lifecycle diagnostics.
What changed: Added timer-completed alarm parsing and MQTT subscription support, configurable completed-topic/alarm settings, endpoint play-sound alarm routing, command session correlation for replay playback, firmware replay session tagging, and focused regression tests/docs.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_timer_announcements.py tests/test_config.py tests/test_voice_websocket.py tests/test_firmware_voice_envelope.py tests/test_api.py::test_voice_status_reports_tts_warmup_background_task` passed; `./firmware/build.sh` built `esp_box_3` and `ha_voice_pe`.
###########
Task #220
Step objective: Add source-agnostic playback interruption and identify the safe path for spoken `stop` during playback.
Issue cause: The firmware playback stack can stop TTS playback, but the command/event naming is TTS-specific and both board profiles pause mic capture while playback is active.
Chosen fix: Add a generic playback stop command/event path now, route existing mute/touch controls through it, and create Task 225 for true on-device stop-word detection because firmware currently has no local keyword engine.
What changed: Added firmware `stop_playback(reason)`, `playback.stop` command handling and event reporting, backend `push_playback_stop_command`, `/api/endpoint/playback/stop`, inbound `playback.stop` history handling, docs, and Task 225 for the local keyword detector dependency.
Verification: `PYTHONPATH=src .venv/bin/pytest -q tests/test_voice_websocket.py tests/test_api.py tests/test_firmware_voice_envelope.py tests/test_config.py tests/test_timer_announcements.py` passed; `./firmware/build.sh` built `esp_box_3` and `ha_voice_pe`.
