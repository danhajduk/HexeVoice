# ESPHome Prototype Archive

This folder preserves the legacy ESPHome prototype configuration.

The active firmware track is now the native ESP-IDF implementation under `firmware/`. The archived ESPHome YAML remains a historical behavior reference for screen states, button behavior, wake-word mode ideas, and device UX parity checks.

Do not use this archive as the current firmware implementation source.
