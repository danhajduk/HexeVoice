# Provider Setup

HexeVoice includes a provider boundary but no real voice provider implementation yet.

Add provider-specific configuration, health checks, APIs, and runtime behavior under:

- `src/hexevoice/providers/`
- `frontend/src/features/providers/`
